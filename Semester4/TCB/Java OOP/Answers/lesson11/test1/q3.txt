public class First {
    protected int num

    public First() {
        this.num = 10; 
    }

    public First(int num) {
        this.num = num;
    }
}

public class Second extends First {
    private double x;
    private First f;

    public Second() {
        super();     
        this.x = 1.1;  
        this.f = null;
    }

    public Second(int num, double x, First f) {
        super(num);
        this.x = x;
        this.f = f;
    }
}

b)
1. compilation error, cant save a parent object in a child variable
2. compilation error, cant downcast a parent to its child without a cast
3. valid
4. valid
5. runtime error, cast exception
6. runtime error, cant cast triangle to circle 
7. valid 
8. compilation error, casting to shape but trying to save as circle