b)
public static void checkInterfaces(Object[] arr) {
      int countG1 = 0;
      int countNoG3 = 0;

      for (int i = 0; i < arr.length; i++) {
            if (arr[i] instanceof Inter1) {
                  countG1++;
            }
            if (!(arr[i] instanceof Inter3)) {
                  countNoG3++;
            }
      }
      System.out.println("Can run g1: " + countG1);
      System.out.println("Cannot run g3: " + countNoG3);
}